import { LatePaymentCharges } from './late-payment-charges';

describe('LatePaymentCharges', () => {
  it('should create an instance', () => {
    expect(new LatePaymentCharges()).toBeTruthy();
  });
});
