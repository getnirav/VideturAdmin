import { AdminUserRolePermission } from './admin-user-role-permission';

describe('AdminUserRolePermission', () => {
  it('should create an instance', () => {
    expect(new AdminUserRolePermission()).toBeTruthy();
  });
});
